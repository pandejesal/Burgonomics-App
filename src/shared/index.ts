/**
 * Shared UI barrel. Reusable UI (components, layouts, navigation, theme,
 * assets) only. Infrastructure has moved to `@/core/*`.
 *
 * Prefer importing from concrete submodules (`@/shared/components`,
 * `@/shared/layouts`, etc.) — this root barrel exists only for tooling
 * that expects a single package entry.
 */
export {};
