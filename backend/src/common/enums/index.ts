export enum SortDirection {
  ASC = 'asc',
  DESC = 'desc',
}

export enum Role {
  CUSTOMER = 'customer',
  STORE_STAFF = 'store_staff',
  STORE_MANAGER = 'store_manager',
  ADMIN = 'admin',
  SUPER_ADMIN = 'super_admin',
}
