import { Module } from '@nestjs/common';

/**
 * Sentry module placeholder. Real initialization is wired in Phase 2
 * once error routing rules are agreed with the SRE team.
 */
@Module({})
export class SentryModule {}
